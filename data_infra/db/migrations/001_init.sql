-- 001_init.sql
-- Initial schema creation (applied via schema.sql on first docker-compose up)
-- This migration is a no-op because schema.sql handles initial table creation.
-- Kept for documentation purposes — future migrations build on this baseline.

-- Tables created by schema.sql:
--   transactions, financial_statements, exceptions,
--   reason_codes, audit_log, benchmark_results
